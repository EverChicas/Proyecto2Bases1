@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>LOG POR MES</title>
    <link type="text/css" href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <script type="text/javascript" src="~/Scripts/bootstrap.min.js"></script>
    <script type="text/javascript" src="~/Scripts/jquery-3.0.0.min.js"></script>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        #form-div {
            position: absolute;
            left: 40%;
            top: 30%;
            border: 1px solid #ccc;
            padding: 10px 10px;
        }

        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        td, th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #dddddd;
        }
    </style>
</head>
<body>

    @*@if (Session["USUARIO"] != null)
        {
            Proyecto2.ClienteWeb.Models.Usuario userLogueado = Session["USUARIO"] as Proyecto2.ClienteWeb.Models.Usuario;*@
    <div class="jumbotron text-center" style="margin-bottom:0">
        <h1>Panel Administrador - Log de Actividades Por Mes</h1>

        @*<p>@userLogueado.Nombre - USUARIO: @userLogueado.Id_Usuario.ToString()</p>*@
        <a href="@Url.Action("vRegresar","Regresar")" class="btn btn-info">Regresar</a>
    </div>
    <div id="form-div">
        <ul class="pagination justify-content-center" style="margin:20px 0">
            <div class="container">
                @using (Html.BeginForm("mostrandoLogMensual", "LogMensual", FormMethod.Post))
                {
                    <div class="form-group">
                        <label for="Mes">Mes:</label>
                        <select id="mes" name="mes">
                            <option value=1>Enero</option>
                            <option value=2>Febrero</option>
                            <option value=3>Marzo</option>
                            <option value=4>Abril</option>
                            <option value=5>Mayo</option>
                            <option value=6>Junio</option>
                            <option value=7>Julio</option>
                            <option value=8>Agosto</option>
                            <option value=9>Septiembre</option>
                            <option value=10>Octubre</option>
                            <option value=11>Noviembre</option>
                            <option value=12>Diciembre</option>
                        </select>
                        <div class="form-group">
                            <label for="Anio">Año:</label> <input type="text" class="form-control" name=anio id="anio" placeholder="ingrese el año">
                        </div>
                    </div>
                        <input id="btn" name="btn" type="submit" value="Consultar" class="btn btn-primary" />
                }
                <a href="@Url.Action("vMostrandoReporteLogM","LogMensual")" class="btn  btn-primary right">Mostrar Reporte</a>
                @*<table>
                        <tr>
                            <td>Fecha-Hora</td>
                            <td>Descripcion</td>
                            <td>Monto</td>
                            <td>Usuario</td>
                        </tr>
                        @{
                            if (Session["LOG_MENSUAL"] != null)
                            {
                                List<Log> lp = Session["LOG_MENSUAL"] as List<Log>;
                                foreach (Log pr in lp)
                                {
                                    <tr>
                                        <td>@pr.FechaHora.ToString()</td>
                                        <td>@pr.Descripcion</td>
                                        <td>@pr.MontoMovimiento.ToString()</td>
                                        <td>@pr.Usuario.ToString()</td>
                                    </tr>
                                }
                            }
                        }
                    </table>*@
            </div>
        </ul>
    </div>
    @*}
        else
        {
            <h2>ERROR: INICIAR SESIÓN</h2>
        }*@
</body>
</html>
